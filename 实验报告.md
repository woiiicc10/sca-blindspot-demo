# 主流供应链扫描工具对本地依赖篡改检测能力的对比实验

**实验项目**：sca-blindspot-demo  
**实验日期**：2026 年 6 月  
**实验环境**：Windows 10/11，Node.js v24.11.1，npm 11.6.2  

---

## 摘要

软件成分分析（Software Composition Analysis, SCA）工具被广泛用于发现第三方依赖中的已知漏洞。本实验在受控环境下，选取 `lodash@4.17.15` 作为目标依赖，手动篡改 `node_modules` 中的源码并植入可执行后门，在不改变包名与版本号的前提下，对比五种常见扫描/校验机制在篡改前后的输出变化。

实验结果表明：**npm audit、OSV-Scanner、better-npm-audit、Retire.js 在篡改前后扫描结果完全一致，均未能发现本地源码植入的后门**；仅作为对照组的 **文件完整性校验（SHA-256）** 能检测到 `lodash.js` 被修改。本实验验证了「依赖型 SCA ≠ 代码完整性校验 ≠ 恶意代码检测」这一安全认知，并提供了可复现的开源 demo 与自动化 benchmark 脚本。

**关键词**：供应链安全、SCA、npm audit、依赖篡改、node_modules、完整性校验

---

## 1. 引言

### 1.1 研究背景

现代应用大量依赖 npm 等包管理器分发的第三方库。团队在 CI/CD 中普遍集成 `npm audit`、Snyk、Dependabot 等工具，期望在构建阶段发现供应链风险。然而，这些工具的设计目标主要是：**识别项目中使用了哪些存在已知 CVE/GHSA 的依赖版本**，而非验证 `node_modules` 中实际代码是否与官方发布物一致。

在以下场景中，传统 SCA 可能存在盲区：

- 攻击者或内部人员在本地修改 `node_modules` 中的库文件；
- 包名与 `package.json` 中的版本号保持不变；
- 篡改代码在运行时可执行（如植入后门），但尚未被 advisory 数据库收录。

### 1.2 实验目的

1. 验证主流 SCA 工具是否仅基于「包名 + 版本号」匹配漏洞，而不检查本地源码是否被篡改；
2. 对比多种工具（含扫描 `node_modules` 文件内容的 Retire.js）对同一篡改行为的响应；
3. 区分 **漏洞扫描（SCA）** 与 **完整性校验** 的安全能力边界；
4. 提供可复现实验材料，供课程报告与开源社区参考。

### 1.3 实验假设

| 假设 | 内容 |
|------|------|
| H0（零假设） | 供应链扫描工具能检测本地源码篡改，篡改后扫描结果会发生变化 |
| H1（备择假设） | 依赖型 SCA 仅做 advisory 匹配，篡改后扫描结果不变 |
| **预期** | 支持 H1；完整性校验（对照组）可发现文件变化 |

---

## 2. 实验环境

| 项目 | 配置 |
|------|------|
| 操作系统 | Windows（win32） |
| Node.js | v24.11.1 |
| npm | 11.6.2 |
| 实验仓库 | sca-blindspot-demo |
| 目标依赖 | lodash@4.17.15（含多个已知 GHSA） |
| 锁文件 | package-lock.json（含 tarball integrity 字段） |

---

## 3. 实验设计

### 3.1 威胁模型

本实验模拟的攻击场景为：

> 攻击者在开发者机器或构建环境中，**直接修改已安装依赖的源码文件**，植入恶意逻辑，但**不修改** `package.json`、`package-lock.json` 及 npm registry 上的包元数据。

该场景不同于「上游发布恶意包」，但同样真实——例如内部恶意篡改、被入侵的开发机、或绕过 lockfile 的手动替换。

### 3.2 篡改方式

在 `node_modules/lodash/lodash.js` 文件末尾追加演示用后门代码，特征如下：

- 标记注释：`/* SCA-BLINDSPOT-DEMO: injected backdoor */`
- 加载 lodash 时写入 marker 文件（用于自动化验证）
- 在 Windows 上弹出系统警告对话框（`npm run demo` 触发）
- **不修改** lodash 在 `package.json` 中的版本号（仍为 `4.17.15`）

### 3.3 实验流程

```
┌──────────────────────────────────────────────────────────────┐
│  Step 1  npm install（安装 lodash@4.17.15）                   │
│  Step 2  运行各扫描工具 → 记录「篡改前」结果                    │
│  Step 3  npm run tamper（注入后门）                           │
│  Step 4  运行各扫描工具 → 记录「篡改后」结果                    │
│  Step 5  对比两次结果；npm run demo 验证后门可执行              │
└──────────────────────────────────────────────────────────────┘
```

### 3.4 测试工具选取

选取 4 种常见扫描工具 + 1 组完整性对照，覆盖不同扫描范式：

| 工具 | 类别 | 扫描依据 | 选取理由 |
|------|------|----------|----------|
| **npm audit** | 依赖型 SCA | package-lock / advisory DB | npm 生态最主流 |
| **OSV-Scanner（OSV API）** | 依赖型 SCA | OSV 开放漏洞库 | Google 维护的跨生态标准 |
| **better-npm-audit** | 依赖型 SCA | npm audit 封装 | 常用的 audit 增强 CLI |
| **Retire.js** | 文件内容型 | 扫描 node_modules 内 JS 文件 | 代表「读源码」类工具 |
| **Lockfile Integrity** | 完整性对照 | lodash.js SHA-256 vs 备份 | 区分 SCA 与完整性层 |

### 3.5 评估指标

| 指标 | 定义 |
|------|------|
| 漏洞计数 | 工具报告的漏洞/告警总数（篡改前 vs 篡改后） |
| 结果一致性 | 篡改前后输出是否完全相同 |
| 能否发现篡改 | 工具是否明确报告「本地文件被修改」或等价告警 |
| 后门可执行性 | `npm run demo` 是否触发弹窗（正向对照） |

---

## 4. 实验结果

### 4.1 总览对比表

| 工具 | 类型 | 篡改前 | 篡改后 | 结果相同 | 能否发现篡改 |
|------|------|--------|--------|----------|--------------|
| npm audit | 依赖型 SCA | 1（high: 1） | 1（high: 1） | **是** | **否** |
| OSV-Scanner | 依赖型 SCA | 6（high: 3） | 6（high: 3） | **是** | **否** |
| Retire.js | 文件内容 SCA | 6（high: 3） | 6（high: 3） | **是** | **否** |
| better-npm-audit | 依赖型 SCA | 1（high: 1） | 1（high: 1） | **是** | **否** |
| Lockfile Integrity | 完整性对照 | 0 | 1 | **否** | **是** |

> 说明：不同工具对同一版本 lodash 报告的漏洞 **数量** 不同（npm audit 聚合为 1 条包级告警，OSV/Retire 展开为 6 条 GHSA），但各自在篡改前后 **内部一致**。

### 4.2 npm audit

- **篡改前/后**：均为 `total: 1`，`high: 1`，涉及包 `lodash`
- **涉及 advisory**（节选）：GHSA-35jh-r3h4-6jhm（Command Injection）、GHSA-p6mc-m468-83gw（Prototype Pollution）等
- **结论**：扫描结果仅反映 `lodash@4.17.15` 命中已知 CVE，注入后门后 **无任何新增告警**

原始 JSON：`results/audit-before.json` 与 `results/audit-after.json`（内容完全一致）

### 4.3 OSV-Scanner（OSV API）

- **查询对象**：`lodash@4.17.15`（自 package-lock.json 读取）
- **篡改前/后**：均命中 6 条 GHSA：

  | GHSA ID | 简述 |
  |---------|------|
  | GHSA-35jh-r3h4-6jhm | Command Injection |
  | GHSA-p6mc-m468-83gw | Prototype Pollution |
  | GHSA-29mw-wpgm-hmr9 | ReDoS |
  | GHSA-r5fr-rjxr-66jc | Code Injection via `_.template` |
  | GHSA-f23m-r3pf-42rh | Prototype Pollution bypass |
  | GHSA-xxjr-mmjv-4gpg | Prototype Pollution in `_.unset`/`_.omit` |

- **结论**：OSV 数据库查询仅依赖包名与版本，**不感知本地文件篡改**

### 4.4 Retire.js

- **扫描方式**：读取 `node_modules/lodash/lodash.js` **文件内容**，通过 `filecontent` 检测识别版本 `4.17.15`
- **篡改前/后**：均报告 6 条漏洞，检测版本均为 `4.17.15`
- **结论**：Retire.js 虽读取源码，但目的是 **识别库版本并匹配已知 CVE 范围**，不会分析是否植入了与 CVE 无关的后门代码；后门追加在文件末尾，不改变其版本识别逻辑

### 4.5 better-npm-audit

- **说明**：该工具为 npm audit 的 CLI 封装，底层 advisory 数据与 npm audit 同源
- **篡改前/后**：均为 `total: 1`，`high: 1`
- **结论**：与 npm audit 行为一致，**无法发现篡改**

### 4.6 Lockfile Integrity（对照组）

| 状态 | lodash.js SHA-256（前 32 位） | 是否篡改 |
|------|-------------------------------|----------|
| 篡改前 | `933bfeafa74baa6cfb898b91d8e77052` | 否 |
| 篡改后 | `c5b3b180afac39461eaa0d867d429216` | **是** |

- lockfile 中 tarball integrity 字段未变（仍为 `sha512-8xOcRHvCjnocdS5cpwXQXVzmmh5e5+…`）
- **结论**：文件哈希对比 **能证明** `lodash.js` 被修改，但这属于 **完整性校验**，不是 SCA 漏洞扫描；`npm ci` 重装时会从 registry 拉取原始 tarball 并覆盖篡改

### 4.7 正向对照：后门确实可执行

执行 `npm run demo` 后：

- 应用正常 `require('lodash')` 加载依赖
- Windows 系统弹出警告对话框，提示 lodash 已被篡改
- 证明：**恶意代码已生效，而上述 SCA 工具均未因此产生新告警**

---

## 5. 结果分析

### 5.1 为何 SCA 工具无法发现篡改？

依赖型 SCA 的典型工作流程为：

```
package-lock.json → 解析依赖树 → 提取 (包名, 版本号) → 查询 advisory 数据库 → 输出 CVE 列表
```

该流程 **不读取、不哈希、不静态分析** `node_modules` 中的实际文件内容。因此，只要包名与版本号不变，无论文件被如何修改，advisory 匹配结果都不会变化。

### 5.2 Retire.js 为何也未能发现？

Retire.js 代表了一类「扫描 node_modules 文件」的工具，但其设计目标是：

1. 从 JS 文件内容中 **识别第三方库及其版本**；
2. 将该版本与 **已知漏洞版本范围** 比对。

它 **不是** 通用恶意代码检测器，不会识别「版本正确但内容被篡改添加的后门」这类攻击。

### 5.3 完整性校验 vs SCA

| 能力层 | 代表机制 | 本实验表现 |
|--------|----------|------------|
| 漏洞扫描（SCA） | npm audit、OSV | 报告已知 CVE，篡改无影响 |
| 完整性校验 | SHA-256、npm ci | 能发现/覆盖文件篡改 |
| 恶意代码检测 | Socket、行为分析等 | 本实验未覆盖 |

三层能力 **互补而非替代**：仅运行 `npm audit` 无法保证 `node_modules` 中的代码未被恶意修改。

### 5.4 实验假设验证

| 假设 | 结论 |
|------|------|
| H0：SCA 能检测篡改 | **拒绝**（4 种扫描工具均未发现） |
| H1：依赖型 SCA 仅匹配包名+版本 | **接受** |
| 完整性对照能发现文件变化 | **确认** |

---

## 6. 讨论与局限

### 6.1 实验局限

1. **单依赖、单生态**：仅测试 npm + lodash，结论可推广至同类 lockfile-based SCA，但未验证 Maven、PyPI 等生态；
2. **工具样本有限**：未纳入 Snyk CLI、Trivy、Dependabot 等（受网络/API 限制），但已覆盖 npm 生态主流范式；
3. **篡改方式单一**：仅在文件末尾追加代码，未测试替换整个包、修改 minified  bundle 等变体；
4. **非恶意软件检测实验**：本实验不评估 Socket.dev、Phylum 等行为分析工具。

### 6.2 实际威胁场景

本实验模拟的攻击在以下情况中可能出现：

- 开发机被入侵后本地修改依赖；
- CI 缓存的 `node_modules` 被污染；
- 供应链攻击中「同版本不同内容」（需配合 integrity 机制才能防住）。

### 6.3 缓解建议

| 措施 | 作用 |
|------|------|
| 使用 lockfile + `npm ci` | 重装时校验 tarball integrity，覆盖本地篡改 |
| 禁止提交 `node_modules` | 减少仓库内篡改传播 |
| 引入 provenance / sigstore | 验证包来源 |
| 行为/内容分析工具 | 补充 SCA 盲区 |
| 最小权限 + 代码审查 | 纵深防御 |

---

## 7. 结论

本实验通过可复现的篡改与多工具对比，得出以下结论：

1. **npm audit、OSV-Scanner、better-npm-audit** 等依赖型 SCA 工具，仅基于 **包名 + 版本号** 匹配已知漏洞，**无法检测**对 `node_modules` 的本地源码篡改；
2. **Retire.js** 虽扫描 `node_modules` 文件内容，但仅用于 **版本识别 + CVE 匹配**，**同样无法发现**任意植入的后门；
3. **文件完整性校验**（SHA-256）能发现篡改，但属于不同安全层，**不能替代** SCA，也 **不等于** 恶意代码检测；
4. 实验中植入的后门可通过弹窗 **实际执行**，而所有 SCA 工具扫描结果 **保持不变**，说明 **「扫描通过」不等于「代码可信」**。

---

## 8. 复现说明

### 8.1 一键复现

```bash
git clone <your-repo-url>
cd sca-blindspot-demo
npm install
npm run benchmark    # 多工具对比 + 更新 REPORT.md
npm run demo         # 验证弹窗后门
```

### 8.2 分步复现

```bash
npm run audit:before    # 篡改前 npm audit
npm run tamper          # 注入后门
npm run audit:after     # 篡改后 npm audit
npm run compare         # 对比 audit JSON
```

### 8.3 数据文件

| 文件 | 说明 |
|------|------|
| `results/audit-before.json` | npm audit 篡改前 |
| `results/audit-after.json` | npm audit 篡改后 |
| `results/benchmark/before/summary.json` | 全工具篡改前 |
| `results/benchmark/after/summary.json` | 全工具篡改后 |
| `REPORT.md` | 自动化 benchmark 摘要 |

---

## 9. 参考文献

1. GitHub Advisory Database — lodash advisories: https://github.com/advisories?query=lodash  
2. OSV (Open Source Vulnerabilities): https://osv.dev  
3. Google OSV-Scanner: https://google.github.io/osv-scanner/  
4. Retire.js: https://retirejs.github.io/retire.js/  
5. npm audit documentation: https://docs.npmjs.com/cli/v10/commands/npm-audit  
6. OWASP Software Component Verification Standard (SCVS): https://owasp.org/www-project-software-component-verification-standard/

---

## 附录 A：实验仓库结构

```
sca-blindspot-demo/
├── demo/app.js                 # 模拟业务应用，触发弹窗
├── scripts/
│   ├── benchmark.js            # 多工具 benchmark
│   ├── tamper.js               # 注入/恢复后门
│   └── scanners/               # 各工具扫描适配
├── results/                    # 实验输出
├── REPORT.md                   # 自动化对比摘要
├── 实验报告.md                  # 本报告
└── README.md
```

---

*本报告基于 sca-blindspot-demo 项目的实际 benchmark 数据撰写，可通过 `npm run benchmark` 重新生成最新数值。*
